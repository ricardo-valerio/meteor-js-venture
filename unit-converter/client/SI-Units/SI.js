if (Meteor.isClient) {

  Template.SI.onCreated(function() {
  });

  Template.SI.onDestroyed(function() {
  });

  Template.SI.onRendered(function() {
  });

  Template.SI.helpers({

  });

  Template.SI.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
