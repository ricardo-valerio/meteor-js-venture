if (Meteor.isClient) {

  Template.magneticField.onCreated(function() {
  });

  Template.magneticField.onDestroyed(function() {
  });

  Template.magneticField.onRendered(function() {
  });

  Template.magneticField.helpers({

  });

  Template.magneticField.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
