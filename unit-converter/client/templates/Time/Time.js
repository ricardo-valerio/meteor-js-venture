if (Meteor.isClient) {

  Template.time.onCreated(function() {
  });

  Template.time.onDestroyed(function() {
  });

  Template.time.onRendered(function() {
  });

  Template.time.helpers({

  });

  Template.time.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
