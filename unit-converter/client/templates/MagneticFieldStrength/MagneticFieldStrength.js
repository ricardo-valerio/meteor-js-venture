if (Meteor.isClient) {

  Template.magneticFieldStrength.onCreated(function() {
  });

  Template.magneticFieldStrength.onDestroyed(function() {
  });

  Template.magneticFieldStrength.onRendered(function() {
  });

  Template.magneticFieldStrength.helpers({

  });

  Template.magneticFieldStrength.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
