if (Meteor.isClient) {

  Template.lengthUnit.onCreated(function() {
  });

  Template.lengthUnit.onDestroyed(function() {
  });

  Template.lengthUnit.onRendered(function() {
  });

  Template.lengthUnit.helpers({

  });

  Template.lengthUnit.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
