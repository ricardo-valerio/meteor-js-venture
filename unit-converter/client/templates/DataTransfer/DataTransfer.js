if (Meteor.isClient) {

  Template.dataTransfer.onCreated(function() {
  });

  Template.dataTransfer.onDestroyed(function() {
  });

  Template.dataTransfer.onRendered(function() {
  });

  Template.dataTransfer.helpers({

  });

  Template.dataTransfer.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
