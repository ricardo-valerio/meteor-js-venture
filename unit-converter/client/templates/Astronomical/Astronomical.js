if (Meteor.isClient) {

  Template.astronomical.onCreated(function() {
  });

  Template.astronomical.onDestroyed(function() {
  });

  Template.astronomical.onRendered(function() {
  });

  Template.astronomical.helpers({

  });

  Template.astronomical.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
