if (Meteor.isClient) {

  Template.molarMass.onCreated(function() {
  });

  Template.molarMass.onDestroyed(function() {
  });

  Template.molarMass.onRendered(function() {
  });

  Template.molarMass.helpers({

  });

  Template.molarMass.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
