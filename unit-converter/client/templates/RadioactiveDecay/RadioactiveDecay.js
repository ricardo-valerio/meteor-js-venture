if (Meteor.isClient) {

  Template.radioactiveDecay.onCreated(function() {
  });

  Template.radioactiveDecay.onDestroyed(function() {
  });

  Template.radioactiveDecay.onRendered(function() {
  });

  Template.radioactiveDecay.helpers({

  });

  Template.radioactiveDecay.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
