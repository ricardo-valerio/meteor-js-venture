if (Meteor.isClient) {

  Template.illuminance.onCreated(function() {
  });

  Template.illuminance.onDestroyed(function() {
  });

  Template.illuminance.onRendered(function() {
  });

  Template.illuminance.helpers({

  });

  Template.illuminance.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
