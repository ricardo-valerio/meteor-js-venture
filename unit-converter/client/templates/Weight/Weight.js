if (Meteor.isClient) {

  Template.weight.onCreated(function() {
  });

  Template.weight.onDestroyed(function() {
  });

  Template.weight.onRendered(function() {
  });

  Template.weight.helpers({

  });

  Template.weight.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
