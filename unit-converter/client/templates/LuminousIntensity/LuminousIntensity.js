if (Meteor.isClient) {

  Template.luminousIntensity.onCreated(function() {
  });

  Template.luminousIntensity.onDestroyed(function() {
  });

  Template.luminousIntensity.onRendered(function() {
  });

  Template.luminousIntensity.helpers({

  });

  Template.luminousIntensity.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
