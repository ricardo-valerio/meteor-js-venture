if (Meteor.isClient) {

  Template.force.onCreated(function() {
  });

  Template.force.onDestroyed(function() {
  });

  Template.force.onRendered(function() {
  });

  Template.force.helpers({

  });

  Template.force.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
