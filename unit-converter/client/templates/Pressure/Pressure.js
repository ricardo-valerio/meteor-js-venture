if (Meteor.isClient) {

  Template.pressure.onCreated(function() {
  });

  Template.pressure.onDestroyed(function() {
  });

  Template.pressure.onRendered(function() {
  });

  Template.pressure.helpers({

  });

  Template.pressure.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
