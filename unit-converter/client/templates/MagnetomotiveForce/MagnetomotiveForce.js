if (Meteor.isClient) {

  Template.magnetomotiveForce.onCreated(function() {
  });

  Template.magnetomotiveForce.onDestroyed(function() {
  });

  Template.magnetomotiveForce.onRendered(function() {
  });

  Template.magnetomotiveForce.helpers({

  });

  Template.magnetomotiveForce.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
