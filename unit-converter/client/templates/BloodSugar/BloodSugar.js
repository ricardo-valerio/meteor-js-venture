if (Meteor.isClient) {

  Template.bloodSugar.onCreated(function() {
  });

  Template.bloodSugar.onDestroyed(function() {
  });

  Template.bloodSugar.onRendered(function() {
  });

  Template.bloodSugar.helpers({

  });

  Template.bloodSugar.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
