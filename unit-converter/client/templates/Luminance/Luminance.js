if (Meteor.isClient) {

  Template.luminance.onCreated(function() {
  });

  Template.luminance.onDestroyed(function() {
  });

  Template.luminance.onRendered(function() {
  });

  Template.luminance.helpers({

  });

  Template.luminance.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
