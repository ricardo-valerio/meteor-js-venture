if (Meteor.isClient) {

  Template.dinamicViscosity.onCreated(function() {
  });

  Template.dinamicViscosity.onDestroyed(function() {
  });

  Template.dinamicViscosity.onRendered(function() {
  });

  Template.dinamicViscosity.helpers({

  });

  Template.dinamicViscosity.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
