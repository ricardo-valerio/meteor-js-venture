if (Meteor.isClient) {

  Template.why.onCreated(function() {
  });

  Template.why.onDestroyed(function() {
  });

  Template.why.onRendered(function() {
  });

  Template.why.helpers({

  });

  Template.why.events({

  });
}

if (Meteor.isServer) {
  Meteor.startup(function () {
    // code to run on server at startup
  });
}
